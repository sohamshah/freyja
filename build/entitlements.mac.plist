<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron's V8 JIT requires this -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Required for Electron's renderer process and native addons -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow loading the bundled Python runtime and freyja_native .so
         which are ad-hoc signed rather than signed with our identity -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
